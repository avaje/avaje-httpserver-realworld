server.port=8080

datasource.driver-class-name=${POSTGRES_DRIVER:org.postgresql.Driver}
datasource.url=${POSTGRES_URL:jdbc:postgresql:postgres}
datasource.username=${POSTGRES_USERNAME:postgres}
datasource.password=${POSTGRES_PASSWORD:postgres}